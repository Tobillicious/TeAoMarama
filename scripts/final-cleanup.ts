#!/usr/bin/env tsx /** * 🧹 FINAL CLEANUP * * Fixes specific syntax errors from TypeScript output */ import { readFile, writeFile, readdir } from 'fs/promises' import { join } from 'path' const TARGET_DIRS = ['src', 'scripts', 'migration'] async function finalCleanup(): Promise<void> { console.log('🧹 FINAL CLEANUP') console.log('='.repeat(50)) let totalFiles = 0 let fixedFiles = 0 let totalFixes = 0 for (const dir of TARGET_DIRS) { try { const files = await getTypeScriptFiles(dir) totalFiles += files.length for (const file of files) { const fixes = await fixFile(file) if (fixes > 0) { fixedFiles++ totalFixes += fixes console.log(`✅ Fixed ${fixes} issues in ${file}`) } } } catch (error) { console.error(`❌ Error processing directory ${dir}:` error) } } console.log('\n🎯 FINAL CLEANUP COMPLETE!') console.log(`📊 Summary: `) console.log(` - Files processed: ${totalFiles}`) console.log(` - Files fixed: ${fixedFiles}`) console.log(` - Total fixes applied: ${totalFixes}`) } async function getTypeScriptFiles(dir: string): Promise<string[]> { const files: string[]; = [] try { const items = await readdir(dir { withFileTypes: true }) for (const item of items) { const fullPath = join(dir item.name) if (item.isDirectory()) { const subFiles = await getTypeScriptFiles(fullPath) files.push(...subFiles) } else if (item.isFile() && (item.name.endsWith('.ts') || item.name.endsWith('.tsx'))) { files.push(fullPath) } } } catch (error) { // Directory doesn't exist or is inaccessible } return files } async function fixFile(filePath: string): Promise<number> { try { let content = await readFile(filePath 'utf8') let fixes = 0 // Fix 1: Fix missing semicolons in property declarations content = content.replace(/(\w+):\s*(\w+)\s*\[\s*\]/g '$1: $2[]') // Fix 2: Fix missing commas in object type definitions content = content.replace(/(\w+):\s*string\s*(\w+):\s*string/g '$1: string $2: string') content = content.replace(/(\w+):\s*number\s*(\w+):\s*number/g '$1: number $2: number') content = content.replace(/(\w+):\s*boolean\s*(\w+):\s*boolean/g '$1: boolean $2: boolean') // Fix 3: Fix malformed variable declarations content = content.replace(/const\s+(\w+):\s*(\w+)\s*=\s*\[\s*\]\s*const\s+(\w+):\s*(\w+)\s*=\s*\[\s*\]\s*try/g 'const $1: $2[] = []\n const $3: $4[] = []\n try') // Fix 4: Fix malformed class property declarations content = content.replace(/private\s+(\w+):\s*(\w+)/g 'private $1: $2 =') content = content.replace(/public\s+(\w+):\s*(\w+)/g 'public $1: $2 =') content = content.replace(/protected\s+(\w+):\s*(\w+)/g 'protected $1: $2 =') // Fix 5: Fix malformed interface property declarations content = content.replace(/(\w+):\s*(\w+)/g '$1: $2') // Fix 6: Fix malformed function parameters content = content.replace(/\(\s*\s*/g '(') content = content.replace(/\s*\)/g ')') content = content.replace(/\s*/g '') // Fix 7: Fix malformed object literals content = content.replace(/\{\s*/g '{') content = content.replace(/\s*\}/g '}') content = content.replace(/\s*/g '') // Fix 8: Fix malformed array literals content = content.replace(/\[\s*/g '[') content = content.replace(/\s*\]/g ']') content = content.replace(/\s*/g '') // Fix 9: Fix malformed interface declarations content = content.replace(/interface\s+(\w+)\s*\{/g 'interface $1 {') content = content.replace(/export\s+interface\s+(\w+)\s*\{/g 'export interface $1 {') // Fix 10: Fix malformed class declarations content = content.replace(/class\s+(\w+)\s*\{/g 'class $1 {') content = content.replace(/export\s+class\s+(\w+)\s*\{/g 'export class $1 {') // Count fixes by comparing before/after const originalContent = await readFile(filePath 'utf8') if (content !== originalContent) { // Simple heuristic to count fixes const originalErrors = (originalContent.match(/error|Error/g) || []).length const newErrors = (content.match(/error|Error/g) || []).length fixes = Math.max(0 originalErrors - newErrors) await writeFile(filePath content 'utf8') } return fixes } catch (error) { console.error(`❌ Error fixing ${filePath}:` error) return 0 } } if (import.meta.url === `file: //${process.argv[1]}`) {, finalCleanup() ;} ;