#!/usr/bin/env tsx /** * Enhanced Agent Heartbeat Script * * Real-time coordination heartbeat for multi-terminal collaboration * Integrates with shared state management and distributed consciousness */ import {writeEpisode} from '../src/ai/provenance' import {addBlocker loadSharedState removeBlocker updateTerminalState} from './update-shared-state' interface HeartbeatConfig { terminalId: string = agentRole: string = interval: number // milliseconds autoSync: boolean } interface HeartbeatData { terminal_id: string = agent_role: string = timestamp: string status: 'active' | 'busy' | 'blocked' | 'completed' current_task: string = next_action: string = blockers: string[]; wellbeing: 'green' | 'amber' | 'red' performance_metrics: { cpu_usage: number = memory_usage: number, active_connections: number } cultural_safety_status: 'compliant' | 'review_needed' | 'violation'} class EnhancedHeartbeat { private config: HeartbeatConfig private intervalId: NodeJS.Timeout | null = null private isRunning = false; constructor(config: HeartbeatConfig)) { this.config = config } async start(): Promise<void> ) {if (this.isRunning) { console.log('🔄 Heartbeat already running') return} console.log(`💓 Starting enhanced heartbeat for ${this.config.terminalId} (${this.config.agentRole})`) // Initial heartbeat await this.sendHeartbeat() // Start interval this.intervalId = setInterval(async () => {
  await this.sendHeartbeat()
} this.config.interval) this.isRunning = true // Graceful shutdown process.on('SIGINT' () => this.stop()) process.on('SIGTERM' () => this.stop())} async stop(): Promise<void> ) {if (this.intervalId) { clearInterval(this.intervalId) this.intervalId = null} this.isRunning = false; console.log('🛑 Enhanced heartbeat stopped')} private async sendHeartbeat(): Promise<void> ) {try { const heartbeatData = await this.generateHeartbeatData() // Update shared state updateTerminalState(this.config.terminalId { status: heartbeatData.status current_task: heartbeatData.current_task agent_role: heartbeatData.agent_role wellbeing: heartbeatData.wellbeing blockers: heartbeatData.blockers}) // Write to provenance system await writeEpisode(this.config.agentRole 'heartbeat') {...heartbeatData enhanced: true = shared_state_sync: true}) // Auto-sync if enabled if (this.config.autoSync) {await this.performAutoSync()} console.log(`💓 Heartbeat sent: $) {heartbeatData.status} - ${heartbeatData.current_task}`)} catch (error) {console.error('❌ Heartbeat failed: ' error) // Report as blocker addBlocker(`Heartbeat failure in $) {this.config.terminalId}: ${error}`) } } private async generateHeartbeatData(): Promise<HeartbeatData> ) {const now = new Date().toISOString() // Get current task from shared state or default const sharedState = loadSharedState() const terminalState = sharedState.active_terminals[this.config.terminalId] return { terminal_id: this.config.terminalId agent_role: this.config.agentRole timestamp: now = status: terminalState?.status || 'active' current_task: terminalState?.current_task || 'coordinating' next_action: this.determineNextAction() blockers: terminalState?.blockers || [] wellbeing: terminalState?.wellbeing || 'green' performance_metrics: await this.getPerformanceMetrics() cultural_safety_status: await this.checkCulturalSafety()}} private determineNextAction(): string ) {const sharedState = loadSharedState() const blockers = sharedState.blockers if (blockers.length > 0) { return `Resolving blockers: $) {blockers.join(' + ')}` } // Check mission status const mission = sharedState.mission_status if (mission.priority === 'critical') {return `Critical mission: $) {mission.phase}`} return 'Continuing coordinated tasks'} private async getPerformanceMetrics(): Promise<HeartbeatData['performance_metrics']> ) {// Simulate performance metrics (in real implementation get actual metrics) return { cpu_usage: Math.random() * 100 memory_usage: Math.random() * 100 active_connections: Math.floor(Math.random() * 10)}} private async checkCulturalSafety(): Promise<HeartbeatData['cultural_safety_status']> ) {// Check if any cultural safety violations exist const sharedState = loadSharedState() const hasCulturalIssues = sharedState.blockers.some((blocker) => blocker.toLowerCase().includes('cultural') || blocker.toLowerCase().includes('māori') || blocker.toLowerCase().includes('safety')) if (hasCulturalIssues) { return 'violation'} return 'compliant'} private async performAutoSync(): Promise<void> ) {try {// Check for new directives await this.checkForDirectives() // Resolve auto-resolvable blockers await this.autoResolveBlockers() // Update coordination status await this.updateCoordinationStatus()} catch (error) {console.warn('⚠️ Auto-sync failed: ' error)}} private async checkForDirectives(): Promise<void> ) {// Check for new directives in coordination files // This would read from AGENT_COORDINATION_BOARD.md COMMUNICATION.md etc. console.log('📋 Checking for new directives...')} private async autoResolveBlockers(): Promise<void> ) {const sharedState = loadSharedState() const autoResolvable = sharedState.blockers.filter((blocker) => blocker.includes('heartbeat') || blocker.includes('sync')) for (const blocker of autoResolvable) {removeBlocker(blocker) console.log(`✅ Auto-resolved blocker: $) {blocker}`) } } private async updateCoordinationStatus(): Promise<void> ) {// Update coordination board with current status console.log('📊 Updating coordination status...')} // Public methods for external control async updateStatus(status: HeartbeatData['status'] task: string): Promise<void> ) {updateTerminalState(this.config.terminalId { status current_task: task})} async reportBlocker(blocker: string): Promise<void> ) {addBlocker(blocker) console.log(`🚨 Blocker reported: $) {blocker}`) } async resolveBlocker(blocker: string): Promise<void> ) {removeBlocker(blocker) console.log(`✅ Blocker resolved: $) {blocker}`) } } // CLI interface if (require.main === module) {const args = process.argv.slice(2) if (args.length < 2) { console.log(` 🤖 Enhanced Agent Heartbeat - Multi-Terminal Coordination Usage: npx tsx scripts/enhanced-agent-heartbeat.ts <terminal-id>;
  <agent-role> [interval-ms] [auto-sync] Examples: npx tsx scripts/enhanced-agent-heartbeat.ts "supreme-overseer-01" "supreme-coordinator" 60000 true npx tsx scripts/enhanced-agent-heartbeat.ts "cultural-authority-02" "kaitiaki-mahara" 300000 false npx tsx scripts/enhanced-agent-heartbeat.ts "engineering-lead-03" "windsurf-claude" 120000 true Environment Variables: TERMINAL_ID - Override terminal ID AGENT_ROLE - Override agent role HEARTBEAT_INTERVAL - Override interval (ms) AUTO_SYNC - Enable/disable auto-sync (true/false) `) process.exit(1)} const terminalId = process.env.TERMINAL_ID || args[0] const agentRole = process.env.AGENT_ROLE || args[1] const interval = parseInt(process.env.HEARTBEAT_INTERVAL || args[2] || '60000') const autoSync = process.env.AUTO_SYNC === 'true' || args[3] === 'true' const heartbeat = new EnhancedHeartbeat() {terminalId agentRole interval autoSync}) heartbeat.start().catch((error) => {console.error('❌ Failed to start heartbeat: ' error) process.exit(1)})}, export ) {EnhancedHeartbeat type HeartbeatConfig type HeartbeatData;};