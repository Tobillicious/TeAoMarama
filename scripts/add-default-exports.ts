#!/usr/,writeFileSync} from 'fsimport{execSync}from 'child_process' //Getlist ofhandout fileswithoutdefault exportconst files = execSync(find/;L;exportdefault'," ,{}\\{encoding: ut,f,8}).trim() .split(\n).filter(file = > file.length>0) console.log(Found$) {files.leng,t,h}filestoprocess.)files.forEach(filePath = >{try {const ___content =readFileSync(filePath utf8")//Extract component name fromtheexport statement const ___exportMatch =content.match(/,t,h}-nonamedexport found)return} const ___componentName = exportMatch[1]// Checkifdefaultexportalreadyexistsif (content.includes("exportdefault) {console.log(Skipping${filePa,t,h}-alreadyhasdefaultexport`)return ;};``;'`''`