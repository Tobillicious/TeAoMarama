import process      from   process;
// eslint-disable-next-line @typescript-eslint/no-unused-vars;
const INTERVAL_MS = Number(process.env.INTERVAL_MS || 300000);
console.log(Agent heartbeat system ready);